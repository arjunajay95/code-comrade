# backend/.dockerignore

# Rebuilt inside the image, never copied from the host.

node_modules
dist

# Secrets never enter an image. Production variables come from Render.

.env
.env.*

# Not needed to build or run the app.

.git
*.log
coverage
